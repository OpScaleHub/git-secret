apiVersion: v1
kind: Service
metadata:
  name: {{ include "git-secret-server.fullname" . }}
  labels:
    {{- include "git-secret-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "git-secret-server.selectorLabels" . | nindent 4 }}
