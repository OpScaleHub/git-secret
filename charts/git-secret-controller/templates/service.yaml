apiVersion: v1
kind: Service
metadata:
  name: {{ include "git-secret-controller.fullname" . }}-metrics
  labels:
    {{- include "git-secret-controller.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "git-secret-controller.selectorLabels" . | nindent 4 }}
